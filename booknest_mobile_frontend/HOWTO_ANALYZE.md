From this directory (Flutter app root), run:
- flutter pub get
- flutter analyze

If invoked from repository root, use:
- bash RUN_ANALYZE_ABS.sh
or
- bash RUN_ANALYZE.sh
or
- bash RUN_ANALYZE_STRICT.sh
