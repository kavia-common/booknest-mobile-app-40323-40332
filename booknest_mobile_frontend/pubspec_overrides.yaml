dependency_overrides:
  # These overrides ensure that when Flutter tools are invoked from the repository root,
  # dependencies resolve consistently with the nested Flutter project. This helps analyzers
  # that insist on running from repo root to find the same dependency graph.
  sqflite: ^2.3.2
  path: ^1.9.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  flutter_staggered_grid_view: ^0.7.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
